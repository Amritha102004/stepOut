<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StepOut - Forgot Password</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            background-color: #f8f9fa;
        }

        .container {
            display: flex;
            width: 100%;
            height:400px;
            max-width: 800px; 
            margin: auto;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            overflow: hidden;
        }

        .branding {
            position: relative;
            flex: 1;
            background-color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 40px;
            overflow: hidden;
        }

        .logo {
            position: absolute;
            top: 24px;
            font-size: 2rem;
            font-style: italic;
            font-weight: 300;
            z-index: 2;
        }

        .gradient-circle {
            width: 280px;
            height: 280px;
            border-radius: 50%;
            background: linear-gradient(135deg, #9c27b0, #ff9800);
            position: relative;
            z-index: 1;
        }

        .sneaker-img {
            position: absolute;
            width: 320px;
            height: auto;
            transform: rotate(-15deg);
            z-index: 2;
        }

        .form-container {
            flex: 1;
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background-color: white;
        }

        h1 {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #333;
        }

        p {
            margin-bottom: 30px;
            color: #666;
            font-size: 0.95rem;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-size: 0.9rem;
        }

        input {
            width: 100%;
            padding: 12px 16px;
            margin-bottom: 25px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
        }

        button {
            width: 100%;
            background-color: #4285f4;
            color: white;
            border: none;
            padding: 14px;
            border-radius: 25px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #3367d6;
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                box-shadow: none;
                max-width: 100%;
                border-radius: 0;
            }

            .branding {
                padding: 30px 20px 50px;
            }

            .gradient-circle {
                width: 200px;
                height: 200px;
            }

            .sneaker-img {
                width: 220px;
            }

            .form-container {
                padding: 40px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="branding">
            <div class="logo">stepOut</div>
            <div class="gradient-circle"></div>
            <!-- Using a placeholder image for the sneaker -->
            <img class="sneaker-img" src="/api/placeholder/400/320" alt="Stylish sneaker with flame accents">
        </div>
        <div class="form-container">
            <h1>Forgot password</h1>
            <p>Please enter your Email to reset the password</p>
            <% if (error) { %>
                <p class="error"><%= error %></p>
            <% } %>
            <form action="/forgotpassword" method="post">
                <label for="email">email</label>
                <input type="email" id="email" placeholder="Enter your email" name="email">
                <button type="submit">Reset Password</button>
            </form>
        </div>
    </div>
</body>
</html>